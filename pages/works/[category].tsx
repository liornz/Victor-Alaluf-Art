interface Props {

}

const ArtCategoryPage: React.FC<Props> = (props) => {
const {  } = props;
  return (
    <div>
      A specific art category page - sculptures / Painting / Video works / Installations etc.
    </div>
  );
};

export default ArtCategoryPage;