interface Props {

}

const Works: React.FC<Props> = (props) => {
const {  } = props;
  return (
    <div>
      Main Artworks Page with Cards linking to specific categories of the artworks
    </div>
  );
};

export default Works;